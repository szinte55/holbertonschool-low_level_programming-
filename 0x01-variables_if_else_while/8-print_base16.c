#include <stdio.h>
/**
 * main - prints all numbers of base 16 in lowercase
 * Return: Always 0(success)
 */
int main(void)
{
	int h;
	int k;

	for (h = '0'; h <= '9'; h++)
		putchar (a);

	for (k = 'a'; k <= 'f'; k++)
		putchar (k);
	putchar ('\n');
	return (0);
}
